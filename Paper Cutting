#include <bits/stdc++.h>
using namespace std;

int main() {
    
    int tt;
    cin>>tt;
    while(tt--) {
        
        int n,k;
        cin>>n>>k;
        
        int result = n / k;
        int ans = result * result;
        
        cout<<ans<<endl;
    }
}
