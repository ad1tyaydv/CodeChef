#include <bits/stdc++.h>
using namespace std;

int main() {
    
    int tc;
    cin>>tc;
    while(tc--) {
        
        int n;
        cin>>n;
        int num = n % 10;
        
        if(num < 5) {
            cout<<100 - (n - num)<<endl;
        }
        else if(num >= 5) {
            cout<<100 - (n + (10 - num))<<endl;
        }
        else {
            cout<<n<<endl;
        }
    }
}
